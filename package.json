{
  "name": "slack-url-summary-bot",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "dev:test": "wrangler dev --env test",
    "dev:company": "wrangler dev --env company",
    "start": "wrangler dev",
    "test": "vitest",
    "cf-typegen": "wrangler types"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.12.4",
    "@types/node": "^25.5.2",
    "typescript": "^5.5.2",
    "vitest": "~3.2.0",
    "wrangler": "^4.81.0"
  },
  "dependencies": {
    "slack-cloudflare-workers": "^1.3.9"
  }
}
